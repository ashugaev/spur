export const DEFAULT_PROJECT_PREFLIGHT_PROMPT =
  'Check the repo for relevant skills first, then inspect all agent instruction files for branch-naming rules. Return only JSON: {"branch":"<git branch name>"} when you can propose a branch that follows those rules and uses identifiers from the user\'s task prompt when they fit. Return only {"noProjectBranchRequirements":true} when the project defines no branch-naming rules. Do not return prose, markdown, or another JSON shape.';
