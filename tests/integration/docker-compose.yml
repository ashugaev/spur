version: "3.8"

services:
  onboarding-test:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile
    command: /workspace/spur/tests/integration/onboarding-test.sh
    environment:
      - NODE_ENV=test
      - CI=true
