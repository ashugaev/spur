node_modules
node_modules/
dist/
dist-server/
.pnpm-store/
.next/
.next-sidecars/
*.tsbuildinfo
coverage/
*.patch
*-context.md
packages/web/screenshots/
packages/web/test-results/
v2/web/
v2/skills/

# Environment files (secrets)
.env
.env.local
.env.*.local
.env.production.local
.env.development.local
.env.test.local

# Credentials and secrets
*.key
*.pem
*.p12
*.pfx
*.cer
*.crt
*.der
*.csr
secrets.yaml
secrets.yml
credentials.json
credentials.yaml
*-credentials.*
.secrets/
.credentials/

# API keys and tokens
.token
.api-key
*-token.txt
*-api-key.txt

# Cloud provider credentials
.aws/
.gcloud/
.azure/

# SSH keys
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.ppk

# Local Claude Code config (per-worktree, not committed)
.claude/settings.json
.claude/metadata-updater.sh
.claude/worktrees/
.claude/scheduled_tasks.lock

# Optional local Spur config overrides
spur.local.yaml
spur.*.local.yaml

.idea/
.playwright-mcp/

# Cursor per-machine workspace trust state (local only)
.cursor/.workspace-trusted

# Local-only planning docs (not shipped / not public)
docs/workflow-technical-updates.md
docs/context-engineering-backlog.md
.scratch/
