name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          dest: ${{ runner.temp }}/pnpm-setup
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm build
      - run: pnpm test

  playwright:
    name: Playwright E2E
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --dir packages/web exec playwright install chromium
      - run: pnpm --dir packages/web build
      - name: Reserve web port
        run: |
          WEB_PORT="$(node -e 'const net = require("node:net"); const server = net.createServer(); server.listen(0, "127.0.0.1", () => { const address = server.address(); if (!address || typeof address === "string") process.exit(1); console.log(address.port); server.close(); });')"
          echo "WEB_PORT=$WEB_PORT" >> "$GITHUB_ENV"
      - name: Start Next.js
        run: pnpm --dir packages/web exec next start -p "$WEB_PORT" &
      - name: Wait for server
        run: npx wait-on "http://127.0.0.1:$WEB_PORT" --timeout 30000
      - run: pnpm --dir packages/web exec playwright test
        env:
          PLAYWRIGHT_BASE_URL: http://127.0.0.1:${{ env.WEB_PORT }}

  runtime-integration:
    name: Runtime Integration
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          dest: ${{ runner.temp }}/pnpm-setup
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Start tmux server
        run: tmux start-server
      - run: pnpm install --frozen-lockfile
      - run: pnpm --dir v2 build
      - run: pnpm test:integration
